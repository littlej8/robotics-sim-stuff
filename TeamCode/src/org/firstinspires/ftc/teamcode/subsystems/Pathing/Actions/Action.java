package org.firstinspires.ftc.teamcode.subsystems.Pathing.Actions;

/*
 * An action to run at a waypoint in a path. run() is executed repeatedly until it returns false.
 */
public interface Action {
    boolean run();
}
